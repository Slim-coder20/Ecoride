{% extends 'base.html.twig' %}

{% block title %}Connexion | EcoRide
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<div class="row justify-content-center">
			<div class="col-md-6">
				<h2 class="text-center text-success">Connexion</h2>
				<p class="text-center text-dark">Accédez à votre compte pour réserver et proposer des trajets.</p>

				<!-- Affichage des erreurs -->
				{% if error %}
					<div class="alert alert-danger">
						{{ error.messageKey|trans(error.messageData, 'security') }}
					</div>
				{% endif %}

				<div class="card shadow p-4">
					<form method="post">
						<div class="mb-3">
							<label for="username" class="form-label text-success">Email</label>
							<input type="email" id="username" name="_username" class="form-control" value="{{ last_username }}" required autofocus>
						</div>

						<div class="mb-3">
							<label for="password" class="form-label text-success">Mot de passe</label>
							<input type="password" id="password" name="_password" class="form-control" required>
						</div>

						<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

						<div class="mb-3 form-check">
							<input type="checkbox" id="remember_me" name="_remember_me" class="form-check-input">
							<label for="remember_me" class="form-check-label text-success">Se souvenir de moi</label>
						</div>

						<button type="submit" class="btn btn-success w-100">Se connecter</button>
					</form>

					<p class="text-center mt-3 text-success">

						Pas encore de compte ?
						<a href="{{path('app_register')}}" class="text-decoration-none text-success">Inscrivez-vous</a>
						<br>
						<a href="#" class="text-decoration-none text-success">mot de passe oublié</a>
					</p>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
